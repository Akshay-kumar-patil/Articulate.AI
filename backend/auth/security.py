# This module is kept for backwards compatibility but is no longer used.
# Authentication has been replaced with a session-based system (no passwords).
# See backend/routes/auth.py for the new /auth/start-session endpoint.