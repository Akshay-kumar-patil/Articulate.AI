# ─────────────────────────────────────────────
# Backend dependencies (Render / FastAPI server)
# ─────────────────────────────────────────────

# FastAPI + server
fastapi
uvicorn[standard]

# Environment variables
python-dotenv

# Request validation / serialisation
pydantic

# Resume parsing — PDF loader
langchain_community
pymupdf

# Groq SDK (LLM + Whisper speech-to-text)
groq

# File uploads (multipart/form-data)
python-multipart

# ─────────────────────────────────────────────
# Streamlit frontend dependencies
# Install these separately when running the Streamlit UI:
#   pip install streamlit streamlit-mic-recorder streamlit-cookies-manager gtts requests
# They are intentionally excluded here so Render's build is lean and fast.
# ─────────────────────────────────────────────